# Overnight 0.2.5 work — 2026-09-09 UTC

The owner authorized execution of the post-0.2.4 queue. This is local candidate
work; published 0.2.4 stays unchanged. No game drive, hardware output, external
messages or scheduled deployment checks are part of this work.

## USB input identity and rediscovery — KI-21

Legacy duplicate-name and missing-reader regressions fail on the 0.2.4 source;
logs are `results/overnight-025-identity-baseline.log` and
`results/overnight-025-reconnect-baseline.log` (local). New assignments retain a
strict GUID; unambiguous legacy bindings upgrade through deferred persistence.
Ambiguous legacy devices require Assign. Refresh/retry occurs while idle, never
as recurring enumeration during driving. Toolkit v0.12.0 is unchanged.

- 132 input assertions pass, including duplicate/renamed/missing devices,
  reordered enumeration, failed-open ambiguity, XML/Flip persistence, late USB
  discovery and retry throttling. 450,079 force/consumer assertions still pass.
- RC1: `0.2.5-rc.1+c5358b47d0a9ff1cd30206e3870d0a61cbd3b3e1.clean`.
- SHA-256: `A42C2AE454979839BB67ECDDF621635CB88DC354A0EE9AB7D6779A13217CD09F`.
- [Full automated report](../../results/rc-0.2.5-rc.1-023bb92183af4d5db4d579bd1fae7b54/automated.json): all 16 checks pass.
- [Installation receipt](../../results/overnight-025-install-5edae33a279049348d8708b33c49543b/install-receipt.json): exact package installed with game closed,
  six payloads/native copy verified, settings preserved, 0.2.4 backed up, no probe.
- [Attended checklist](../../results/rc-0.2.5-rc.1-023bb92183af4d5db4d579bd1fae7b54/manual.json): pending; TSS/Fanatec/reconnect hardware results not claimed.

## Frame-health retention — KI-19 diagnostics

Bounded (8 KiB), opt-in XML snapshots are written only at idle/shutdown, after
output release. Read the previous snapshot once at mod load and preserve that
identity in subsequent support reports. Current writes cannot be relabelled as
previous-session measurements. Snapshots older than 30 days, corrupt documents,
invalid counters and DTDs are rejected. No frame timeline or crash guarantee.

51 support assertions pass, including real locked-file preservation/retry,
duplicate shutdown, disabled logging, distinct session identities, stale/corrupt
data, bounded log reads and 100,000 allocation-free frame observations. Lifecycle
38 assertions include diagnostic persistence after force/telemetry release.
Warnings-as-errors production build passes. RC2 passes all 16 checks:
`0.2.5-rc.2+c7d9aeedee04d854bcc9636db5926c661f03d372.clean`, SHA-256
`4A6B05390F5E4EF587D561328F525F49C348164DCD4362919A0562586004A887`.
[Automated report](../../results/rc-0.2.5-rc.2-f0c9b14f80844cdd94f508e432a6fbdb/automated.json).
[Install receipt](../../results/overnight-025-install-02e4f9fde8f24753806a60dacf425fe2/install-receipt.json): installed with game closed, payloads verified,
settings unchanged and RC1 backed up. Actual game retention remains an attended check.

## Lifecycle/performance audit

- KI-22: missing axle/wheel data while UNDERWAY retained the previous force.
  Actual callback reproduction fails before correction; 19 lifecycle assertions
  now pass with the shared force arithmetic and fake native output.
- KI-23: shifter startup/picker opened a stale index after enumeration changed.
  Reproduction fails before correction; strict GUID resolution and unique-name
  migration now pass. Close/reopen resets gear latches. Stage-change resets were
  already present; no redundant stage workaround was added.
- KI-24: resuming with a pending assignment could save settings while driving.
  Reproduction fails before correction; assignments cancel, and all direct-input
  edits now defer driving writes and retry locked-file failures while idle.
- Combined direct-input/shifter suite passes 150 assertions. Watchdog/camera
  lifecycle suite passes 39, including shifter/diagnostic persistence after release.
- Confirmed the normal watchdog/input/camera polling paths use cached manager
  metadata, cached components, bounded logs and deferred writes. Device pickers
  cache their lists; direct-input recovery refresh is idle-only. Diagnostic force
  traces remain opt-in and rate-limited. The abandoned Rewired experiment was not
  run. No additional game FPS or crowd-specific diagnosis is claimed.

Local baseline failures are `results/overnight-025-force-lifecycle-baseline.log`,
`results/overnight-025-shifter-baseline.log` and
`results/overnight-025-assignment-resume-baseline.log`.

RC3 passes all 16 local checks:
`0.2.5-rc.3+f9ea41a7b6c7f342eba85086e445999e6877435d.clean`, SHA-256
`33A5DECD6B0332EADCE0CAD3FA4EE974F13D002FCC31149F968BCB148D86ADC2`.
[Automated report](../../results/rc-0.2.5-rc.3-c964f815f90f4f6e9d887556d1d2b01e/automated.json).
[Install receipt](../../results/overnight-025-install-3505356ce8f04270a1ffeb957ffb347a/install-receipt.json): game closed, six payloads/native copy match, settings
unchanged, RC2 backed up, developer probe absent. Attended checks are pending.

## Development motion/contact capture

Separate probe schema 3 adds per-wheel contact, raw compression/capacity,
Rigidbody world/local motion, rotation and fixed-time observations aligned with
each force row. Standalone replay reports debounced landing and slide-recovery
candidates with force comparison/context, bounded event detail and explicit
unknowns. Differentiation occurs in world coordinates before projection, with
reset/gap/teleport/missing-data boundaries. No new production force effects.

Focused validation: 42 recorder assertions, actual net48/Harmony attach-observe-
unpatch (14 assertions), and 31 Python replay/evidence tests pass. Tests cover
schema 1/2 compatibility, schema 3 promotion, missing/corrupt/alignment errors,
contact jitter, rotation, discontinuities and bounded event output. Sampling
buffer tests verify no managed allocation or disk IO in the observation path;
they do not measure Unity probe overhead. Warnings-as-errors probe/replay builds
pass. First real capture and signal freshness remain pending.
[Signal contract and interpretation](../DEVELOPMENT-CAPTURE.md).

See [the queue](../OVERNIGHT-QUEUE.md).
