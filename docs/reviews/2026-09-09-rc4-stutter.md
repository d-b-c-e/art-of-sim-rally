# RC4 drive: stutter and manager-initialization flood

Owner report, 2026-09-08 local / 2026-09-09 UTC: "testing was good overall" but a
strong stutter during one stage, roughly a minute into gameplay around a curve
beside a crowd. Whether it cleared by itself was not explicitly answered. This
is scoped smoke feedback, not a passed camera/input/telemetry scenario matrix.

Installed identity:
`0.2.4-rc.4+38c1bff31ff1ca20696c15a8b7de9298ec04dbf3.clean`.
The previous [install receipt](../../results/rc4-install-7bf2d9692b304b58bbe124f80d43cc4b/install-receipt.json)
ties it to the exact package. The game had exited before inspection. Saved settings
have DiagnosticLogging, direct input, FFB and telemetry enabled; Strength 50,
Smoothing 0.2. UMM loaded one mod, with four input devices including the MOZA R12.
The game log identifies `Japan_Stage_6_Reverse_Dry_70s`; the owner did not name
the car. No separate support bundle or real developer capture was collected.

## Preserved evidence

Local snapshots and derived counts live under
`results/stutter-review-20260909-022600/`; logs/decompiled game code are ignored
and never committed. [Inventory](../../results/stutter-review-20260909-022600/inventory.json),
[log-region counts](../../results/stutter-review-20260909-022600/regions.json),
[native command gaps](../../results/stutter-review-20260909-022600/native-force-gaps.json).

| Observation | Count / significance |
|---|---|
| Player.log | 38,552,119 bytes; 804,013 lines for this launch |
| Failed EventManager construction | 43,806; all before the first force trace, logged in MainMenu |
| Freeroam manager construction | 43,090 attempts |
| Uncaught NullReferenceException | 4,981; sampled stack is ghost data callback → lazy manager getter → Freeroam constructor |
| Cannot connect to destination host | 23,468 total; 10,597 between the first and last force traces |
| ProcessReplayData: Top | 11,636 total; 6,217 between force traces |
| Camera saves | Three before the first force trace, one after the last, none between |
| Native command gap | 1.493s at 21:22:22.020–21:22:23.513 local, between nonzero forces |

The native timestamps identify a suspicious output gap, not a measured frame
duration: unchanged/zero output can also omit commands. Unity log lines have no
wall-clock timestamps, so the exact crowd encounter cannot be aligned. Aggregate
frame counters were only in memory and were lost on exit without a support file.
No repeated native acquisition failure or mod settings-write loop appears in the
driving interval. Network/ghost work continues through that interval.

## Confirmed code defect: KI-20

Inspection of build 17584229 shows that `GameEntryPoint.EventManager` creates a
manager when its private `eventManager` field is null. Its getter excludes scene
index 1; the game's own Update excludes indexes 1 **and 3**. It is not a passive
state accessor usable from a persistent menu watchdog.

`EventManager` construction attempts scene-dependent setup. Its exception handler
starts a ghost replay request before logging the failed construction. Derived
Freeroam construction then throws, leaving the singleton unassigned. Our state
checks catch that exception and retry next frame. Ghost completion can enter the
same getter, amplifying the work. These read paths exist in 0.2.3; RC4 added a
diagnostic state read, so this is not evidence of a wholly new RC4 defect.

The correction reads the existing private field through cached reflection metadata.
It never calls the lazy getter and never caches a manager across stage teardown.
Input and support access use the same path. Missing state keeps force/telemetry
parked and leaves the game's input untouched. No game manager creation logic,
leaderboard code, physics, assist, FFB tuning or toolkit binary is changed.

## Verification and retest

- Before the change, the new production-predicate regression reproduced 4,000
  lazy getter invocations/queued requests from 1,000 polls. Afterward: zero;
  all 40 state/lifecycle assertions pass.
- The net48 integration runner checks the actual game's private field with an
  uninitialized concrete stage manager: 23 additional checks, 59 including the
  unchanged transport checks. No Unity constructor is run.
- Direct-input 90 and camera/watchdog lifecycle 36 assertions pass; warnings-as-errors
  build passes. The full successor candidate report will be recorded here.
- RC4's attended stutter case is failed with the report and log evidence. Other
  cases remain pending. The new source is not an attended stutter fix yet.

For the next exact candidate, wait in the main menu for 30 seconds, then drive
the same stage past the same crowd/curve. Verify Player.log no longer fills with
manager initialization errors or a ghost-download backlog. Return to the menu,
repeat once, then create a support file while paused **before quitting**. Retain
ordinary camera/input/FFB/telemetry checks because their state gating is affected.
Do not change crowd/graphics settings before this comparison; no crowd-specific
error or causal measurement was found. RC4 remains installed until explicitly
updated; neither a new install nor publication is implied by this log review.
