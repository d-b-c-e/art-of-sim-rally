# RC10 menu smoke and layout correction

Exact installed candidate: `0.2.6-rc.10+2ab6fb5f27be570693ebe856b48259456f55007f.clean`.
The 20:16–20:22 UTC serialized slot used saved FFB/telemetry Off, no drive,
recording or force test. Game build17584229, UMM0.33.0.0, 3840×2160,
UMM scale1 with default window dimensions (saved as zero).

Evidence: `results/ux-rc10-smoke-a11a727a92124d04a2e095762e4a6874/`.
Original-resolution screenshots preserve the baseline, clipped layout, Down,
Return, first Escape and subsequent native cancel. No resize or crop.

| Check | Observed result |
|---|---|
| F6 opens Simple/Setup | Passed |
| Down with panel open | Career stayed selected; no underlying navigation |
| Return with panel open | Native career screen did not open |
| Escape without pending edit | Panel closed; native Quit did not open |
| Subsequent Escape, Up, Return | Native Quit recovered, selected Yes, game exited normally |
| Default4K layout | **Failed:** larger readable content overflowed the ~960px host; header Stop/Close and later page buttons were outside the viewport; View label wrapped midword |
| All pages, cancel-first binding, held inputs, wheel/pad | Not tested in this run; layout failure shortened the run |

Game absent and exact Settings.xml/UMM/payload/probe hashes restored at
**20:21:00.383 UTC**, before the slot deadline. Lease explicitly released.
Settings SHA `C373DA18429788ACDFEF7A032496981BDCC5571D7285543B249769846E5876EF`;
UMM SHA `7A2A5096DAE00562431CCDE6A2C1E7C6530ACF0B97DF3BF44DACB6FAAD483D7F`.
Crash on19.52381, landing on20 retained. UMM logged successful load with FFB
and telemetry false. Zero opened input controllers does not validate USB input.

## Follow-up

UMM clamps a zero size preference to960×720. RC10 instead treated zero as no
host constraint and set a1720px minimum content width; UMM scrolled that content
inside its unchanged window. Offline screen-bound checks missed this host bound.

The successor reads actual UMM window size without writing host preferences,
fixes outer and nested content widths, wraps pages to three columns at default4K,
and gives view selection its own row. Stop/Close share the title row. Page height
uses the actual/default host height. New fixtures cover zero preferences and
actual960px width; a local assembly check verifies the read-only host-size seam.
All16 local gates passed for **RC11** at20:26:25 UTC, clean source
`82e22a12d55224b6f1d18821bb26b621a281ddcc`. This includes102 settings assertions,
13 actual Unity Mono hook/host-size assertions, both recorded drives and9,507
landing/crash assertions. ZIP SHA-256
`221B2CC0813D72470D3A375FD5DFB62A01030D069A4E84D59B5B65EC09D176E4`.
Gate: `results/rc-0.2.6-rc.11-c060a51ad3d1461ea80864ba7e4e4395/automated.json`,
SHA-256 `FE0D9CEA3E0998AF0CC2CC48B29BFDA66E218783D5A15FA7882CE8F4BD971A16`.
Exact artifacts and122 evidence files are copied/hash-verified in main. **RC11
was not deployed:** independent source review found a further custom-scale issue.
At960px/4x, the sequential shifter row needed1260px inside a770px body. Capping
each child's width did not cap the sum. Gear rows shared the defect.

The RC12 successor stacks binding labels/actions when their aggregate width
cannot fit; camera/axis/calibration rows use the same decision. Device choices
take a full wrapping row. Large-scale toggle/slider actions stack too. Default4K
keeps compact rows. Build and126 settings assertions pass, including row-fit
cases across0.5–5x and960/1280/1920px hosts. Fresh full gate, independent source
closure and a new serialized rendered check remain required.

**RC12 update:** independent source review closed the aggregate row issue at
clean `cbcf76c5d6401dd8a0c511acfe7ded201df6a99d`. The reviewer did not run tests or
the game. All16 local gates subsequently passed; the exact artifact was installed
at20:35:39UTC, game closed, all owner/probe/UMM hashes preserved. See
[the deployment receipt](../LOCAL-DEPLOYMENT.md) for hashes and evidence paths.
The limited rendered check below followed. Toolkit UX guidance12df6b3 and94dc3a1
is recorded in the adoption audit; source closure is not full UX-03 acceptance.

The next menu-only run should first verify Stop/Close and all six pages at4K,
then Simple/Advanced, nested scrolling, camera Bind→Escape cancellation and native
menu recovery. Preserve original-resolution screenshots. Exit normally and restore
the exact saved owner/UMM/probe files before releasing the slot. Other resolution,
custom-scale and physical input checks remain on the broader checklist.

## RC12 rendered follow-up — 20:46–20:52 UTC

Exact installed identity:
`0.2.6-rc.12+cbcf76c5d6401dd8a0c511acfe7ded201df6a99d.clean`.
Evidence: `results/ux-rc12-smoke-d24e3e71d58f498699f4eba1ea8c28f7/`.
The serialized menu-only run used temporary saved FFB/telemetry Off. No output
was enabled, no stage was driven and no recording was requested. UMM preferences
stayed at default dimensions/scale1; screenshots are original3840×2160.

| Check | Observed result |
|---|---|
| Default host header | Readable title, Stop FFB, Close and Simple/Advanced fit |
| Page buttons | All six fit in two rows of three |
| Simple Setup, Controls, FFB, Telemetry, Help | Visited page tops; text wraps within the body, no horizontal clipping observed |
| FFB device status | Missing/unverified saved MOZA selection fits; no device chosen or force enabled |
| Escape without pending edit | Closed only the mod panel; Career remained selected |
| Next Escape, Up, Return | Native Quit recovered and game exited normally |
| Cameras, Advanced, nested scrolling, Bind→Escape | Not completed in the slot; remain pending |
| Held controls, physical USB, alternate scale/resolution | Not tested |

The startup UMM window was dismissed with Escape while the title prompt was
visible; the next observation was the native main menu. That does not establish
whether title input was consumed before or after ownership release. First-use
input isolation needs a separate labelled check; do not treat it as passed.

Restoration completed at **20:51:49.521 UTC**, game absent, with all16 protected
file hashes matching the originals. Settings and UMM SHA-256 values are the same
as the RC10 receipt above; crash on19.52381 and landing on20 are retained.
Lease release was sent at20:51:58UTC, before the deadline. The captured UMM log
confirms FFB/telemetry false, successful mod/probe load and zero opened input
controllers; it provides no physical USB or force evidence. The separate probe
was ready but did not start recording.

The second run below addresses Cameras/Adjustment bindings, cancellation and
nested scrolling. Keep the broader hardware checklist pending.

KI-40 remains open. These narrow keyboard/layout results are not full input, layout,
calibration or physical FFB acceptance. Public0.2.5 and toolkit358add5 unchanged.

## RC12 camera follow-up — 20:58–21:05 UTC

Same installed RC12 identity and default3840×2160/UMM scale1 host. Evidence:
`results/ux-rc12-smoke-74c19cbf1bf5462ead1dad05bbdaff67/`.
FFB and telemetry were temporarily Off; no output, drive or recording occurred.

| Check | Observed result |
|---|---|
| Simple Cameras | Page top fits; Adjustment bindings expands |
| Nested camera scrolling | First keyboard row reached; header/Stop/Close remain accessible, no horizontal clipping observed |
| Bind→Escape, third attempt | **Passed:** Escape requested8,218ms after Bind start, within the10s timeout; active capture canceled, panel stayed open, Career stayed selected and Up:T remained |
| Advanced | Switched successfully; Cameras page top fits. Other Advanced pages were not inspected |
| Normal exit | Escape closed only the settings panel; subsequent Escape/Up/Return exited through native Quit |

The first untimed attempt closed the panel and is inconclusive. The second
timed attempt reached Escape after10,768ms, beyond capture timeout; it cannot
test cancel-first behavior. Both attempts are retained as evidence, not counted
as defects or passes. The third attempt has a timing JSON and an original-size
post-cancel screenshot. Timing is the automation request interval, not a
measurement of Unity event latency; the observed still-open panel establishes
the narrow behavior under test.

Normal exit/restoration completed at **21:05:25.672 UTC**. The allocated slot
ended21:03:30UTC; context compaction overran it. On resumption only observation,
normal exit and restoration were performed, then the lease was explicitly
released. All16 protected hashes match. Settings and UMM hashes are unchanged
from the receipts above. Semantic comparison of original/after-smoke settings
found only temporary FFB/telemetry Off; every binding and effect strength stayed
unchanged. The original Settings.xml and Params.xml were restored byte-for-byte.

The log confirms successful mod/probe load with FFB/telemetry false and no
automatic recording. It reports zero opened input controllers, so this run
provides no USB or physical force acceptance. Six original-resolution PNGs,
two timing records, settings snapshots, logs and the restore receipt are retained.

Remaining: full Advanced and custom-scale/720p layouts, active-edit vertical
usability, first-use input ownership, held-key/pad/USB handback, calibration and
physical FFB. KI-39/KI-40 remain open for that broader acceptance; no public
release or force/telemetry changes were made.
