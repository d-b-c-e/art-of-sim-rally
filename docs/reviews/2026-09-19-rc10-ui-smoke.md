# RC10 menu smoke and layout correction

Exact installed candidate: `0.2.6-rc.10+2ab6fb5f27be570693ebe856b48259456f55007f.clean`.
The 20:16–20:22 UTC serialized slot used saved FFB/telemetry Off, no drive,
recording or force test. Game build17584229, UMM0.33.0.0, 3840×2160,
UMM scale1 with default window dimensions (saved as zero).

Evidence: `results/ux-rc10-smoke-a11a727a92124d04a2e095762e4a6874/`.
Original-resolution screenshots preserve the baseline, clipped layout, Down,
Return, first Escape and subsequent native cancel. No resize or crop.

| Check | Observed result |
|---|---|
| F6 opens Simple/Setup | Passed |
| Down with panel open | Career stayed selected; no underlying navigation |
| Return with panel open | Native career screen did not open |
| Escape without pending edit | Panel closed; native Quit did not open |
| Subsequent Escape, Up, Return | Native Quit recovered, selected Yes, game exited normally |
| Default4K layout | **Failed:** larger readable content overflowed the ~960px host; header Stop/Close and later page buttons were outside the viewport; View label wrapped midword |
| All pages, cancel-first binding, held inputs, wheel/pad | Not tested in this run; layout failure shortened the run |

Game absent and exact Settings.xml/UMM/payload/probe hashes restored at
**20:21:00.383 UTC**, before the slot deadline. Lease explicitly released.
Settings SHA `C373DA18429788ACDFEF7A032496981BDCC5571D7285543B249769846E5876EF`;
UMM SHA `7A2A5096DAE00562431CCDE6A2C1E7C6530ACF0B97DF3BF44DACB6FAAD483D7F`.
Crash on19.52381, landing on20 retained. UMM logged successful load with FFB
and telemetry false. Zero opened input controllers does not validate USB input.

## Follow-up

UMM clamps a zero size preference to960×720. RC10 instead treated zero as no
host constraint and set a1720px minimum content width; UMM scrolled that content
inside its unchanged window. Offline screen-bound checks missed this host bound.

The successor reads actual UMM window size without writing host preferences,
fixes outer and nested content widths, wraps pages to three columns at default4K,
and gives view selection its own row. Stop/Close share the title row. Page height
uses the actual/default host height. New fixtures cover zero preferences and
actual960px width; a local assembly check verifies the read-only host-size seam.
Build and102 settings assertions pass. Full gate and rendered retest are pending.

KI-40 remains open. This narrow keyboard result is not full input, layout,
calibration or physical FFB acceptance. Public0.2.5 and toolkit358add5 unchanged.
